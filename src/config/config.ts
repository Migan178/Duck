const prefix: string = "!";
const owners: string = "415135882006495242"; // 여기에 있는 id 는 수정해 주셔야 합니다.
const config: { prefix: string; owners: string | string[] } = {
  prefix,
  owners,
};

export default config;
